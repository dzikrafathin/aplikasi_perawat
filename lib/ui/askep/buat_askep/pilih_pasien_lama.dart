import 'package:flutter/material.dart';

class HalamanPilihPasienLama extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pilih Pasien'),
      ),
    );
  }
}
